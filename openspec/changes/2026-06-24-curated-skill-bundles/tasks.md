## 0. Baseline And Preflight

- [ ] 0.1 [P0][depends:none][I: 当前 `src-tauri/src/skills.rs` + `useSkills` 现状 + `skills-lock.json` + `bundle.resources` 对象 schema + `app_server_cli.rs:586-617` `codex_args_contain_instruction_override` + `claude.rs:814` `build_command` + `settings_core.rs:299-321` `app_settings_change_requires_codex_restart` + `bin/cc_gui_daemon/daemon_state.rs:2618-2660` daemon `skills_list` handler][O: 落 `docs/perf/v0.5.14-curated-skill-baseline.md` 记录 (1) 现有 12 个 source 扫描路径的行为快照 (2) `useSkills` 对外 `SkillOption[]` 契约 (3) `AppSettings` 现有字段 schema (4) `app_settings_change_requires_codex_restart` 当前判定字段集 (5) daemon 与 Tauri 路径的 `skills_list` 调用关系 (6) `bundle.resources` 当前对象 schema 截图][V: 文件存在, 6 段均非空, `git diff` 显示真实代码引用] 落地 baseline, 给后面"不回归"硬门禁留证据。
- [ ] 0.2 [P0][depends:0.1][I: ponytail `AGENTS.md` 全文(github.com/DietrichGebert/ponytail/blob/main/AGENTS.md)][O: 写入 `src-tauri/resources/curated-skills/lazy-senior-dev/SKILL.md`, 顶部加 attribution 注释 `<!-- Upstream: https://github.com/DietrichGebert/ponytail/blob/main/AGENTS.md | License: MIT -->`; 字符数 / token 估算(经验值 chars/3)在 metadata 里登记][V: `wc -c` 与 `metadata.json` 的 `tokenEstimate` 字段偏差 < 15%] 内嵌 ponytail 内容到资产目录。

## 1. Asset Layer (curated-skills 资源 + skills-lock.json 扩展 + build.rs 校验)

- [ ] 1.1 [P0][depends:0.2][I: 1.0 落盘的 `SKILL.md`][O: 新增 `src-tauri/resources/curated-skills/lazy-senior-dev/metadata.json`, schema 严格匹配 design Decision 3/9/10: `{ "name": "lazy-senior-dev", "displayName": "Lazy senior dev", "version": "4.8.1", "description": "...", "icon": "sparkles", "category": "code-style", "tokenEstimate": 1100, "source": "upstream: DietrichGebert/ponytail v4.8.1", "license": "MIT" }`][V: schema 校验通过, 字段非空, license 在白名单(MIT/Apache-2.0/BSD-2-Clause/BSD-3-Clause/ISC;**MPL-2.0 暂不收**)] 写 metadata.json。
- [ ] 1.2 [P0][depends:1.1][I: `SKILL.md` 真实 sha256 + metadata.json + design Decision 3 `kind: "curated"` 模式][O: 改顶层 `skills-lock.json`, 顶层加 `"version": 2`; 现有 9 个 entry 加 `"kind": "bundled"`; 新增 `lazy-senior-dev` entry, 字段 `kind: "curated"` / `assetPath` / `metadataPath` / `computedHash`(真实 sha256) / `tokenEstimate` / `minClientVersion: "0.5.14"`; computedHash 通过 `shasum -a 256 SKILL.md` 算出][V: `python3 -c "import json; json.load(open('skills-lock.json'))"` 解析成功; sha256 与 `shasum` 命令输出一致; 现有 9 个 entry 加 `kind: "bundled"` 后字段不丢失] 写/扩展 lock 清单。
- [ ] 1.3 [P0][depends:1.2][I: `src-tauri/Cargo.toml` + 现有 `src-tauri/build.rs`(如有)][O: 改/新增 `src-tauri/build.rs` 实现 `validate_curated_skills_lock()`: 读 lock → 只对 `kind == "curated"` 的 entry 重算 sha256, `kind == "bundled"` 或无 `kind` 字段的 entry **跳过校验**; 不一致则 `compile_error!("curated skill lock hash mismatch for {}: expected {}, got {}", name, lock_hash, actual_hash)`; 检查 metadata.json schema 完整性 + license 白名单 + icon 字段 kebab-case ASCII(粗校验); 检查 `assetPath` 不含 `..` 或绝对路径; 加 `println!("cargo:rerun-if-changed=src-tauri/resources/curated-skills")` + `rerun-if-changed=skills-lock.json` 限定监听范围][V: 故意改 SKILL.md 一行后 `cargo check` 失败(compile_error 输出含文件名 + 期望 vs 实际 hash); 改回后 `cargo check` 0; 故意篡改一个 `kind: "bundled"` entry 的 computedHash 后 `cargo check` **不报错** (证明只校验 curated)] 编译时 hash 校验生效。
- [ ] 1.4 [P0][depends:1.3][I: `tauri.conf.json` 现有 `bundle.resources` 对象 schema + design Decision 1][O: 改 `tauri.conf.json` 的 `bundle.resources` 对象, 追加 `"resources/curated-skills/**/*": "curated-skills/"` 和 `"../skills-lock.json": "."`(注意是对象, key=glob, value=destination dir; **不能**走绝对路径; 不能改数组, 现有 3 个 key 保留)][V: `npm run tauri:dev` 启动后, `app.path().resource_dir()` 输出包含 `curated-skills/lazy-senior-dev/SKILL.md` 真实存在; 现有 3 个 key 行为不回归] Tauri 资源打包生效。
- [ ] 1.5 [P1][depends:1.4][I: 1.1-1.4 落盘内容 + 提案 §4 真实回退路径 (a)(b)(c)][O: 新增 `docs/curated-skill-onboarding.md`, 写明准入规则: token 上限 3000 / license 白名单(MIT/Apache-2.0/BSD-2-Clause/BSD-3-Clause/ISC;**MPL-2.0 走法律评审后单独 change**) / icon kebab-case ASCII 粗校验(V1.1 引入完整白名单) / category 枚举 MVP 4 个(`code-style / ui-design / review / debug`) / 命名冲突规则 / "何时不启用"反向说明; **真实回退路径 3 条**: (a) `compile_error!` 临时改 `compile_warn!`; (b) `tauri.conf.json` 移掉 curated-skills 资源映射 + 删 `kind: "curated"` entry; (c) AppSettings 字段保留但 `set_curated_skill_enabled` 加 flag 软下线][V: 文件存在, 至少 6 个 rule section + 3 个 rollback section 完整] 准入规则 + 回退文档。

## 2. Backend Layer (Rust: 扫描 / IPC / AppSettings / Codex 注入 / Claude 注入 / Daemon 一致性 / Restart 守护)

- [ ] 2.1 [P0][depends:1.4][I: 现有 `src-tauri/src/skills.rs` 的 12 个 source 实现 pattern + `SKILL_SOURCE_*` 12 个常量 + design Decision 3 `kind` 过滤 + Decision 5 curator 拼装][O: 新增 `src-tauri/src/curated_skills.rs`, 实现 `load_curated_skills(lock_path: &Path) -> Result<Vec<CuratedSkillEntry>>`(只对 `kind == "curated"` 解析) + `get_curated_skill_body(name: &str) -> Result<String>` + `list_enabled_curated_skill_bodies(app_settings: &AppSettings) -> Vec<(String, String)>`(按 `enabled_curated_skill_ids` 顺序返回) + `validate_token_estimate(body: &str) -> usize`(chars/3 经验值); 常量 `SKILL_SOURCE_CURATED_BUNDLED: &str = "curated_bundled"`; `validate_icon_name(icon: &str) -> Result<(), String>`(kebab-case ASCII 粗校验)][V: `cargo test --manifest-path src-tauri/Cargo.toml curated_skills` 覆盖 (1) 正常 load 1 个 curated entry (2) `kind: "bundled"` entry 跳过 (3) lock 不存在 → Err (4) body 缺失 → Err (5) token 估算与 chars/3 一致 (6) `validate_icon_name` 拒 emoji 拒 PascalCase] 新建 curated_skills Rust 模块。
- [ ] 2.2 [P0][depends:2.1][I: `src-tauri/src/skills.rs` 现有 `SkillEntry` 结构 + `skills_list_local_core` 签名 + design Decision 7 `enabled` 字段][O: `SkillEntry` 增 `pub(crate) enabled: bool` 字段, `#[serde(default = "default_true")]`, serde rename `enabled`; **必须**扩 `skills_list_local_core` 签名接受 `app_settings: &AppSettings` 参数(2 个 caller 同步: `skills::skills_list_local_for_workspace` + `bin/cc_gui_daemon/daemon_state.rs:2624`); 合并 curated 桶后, 遍历 `app_settings.enabled_curated_skill_ids` 集合, 对 curated entry 设 `enabled = true`; 其它 curated 设 `enabled = false`; **非 curated entry 保持 `enabled = true`**][V: 现有 `skills.rs` 单元测试全 pass; 新增测试 case: (1) 1 个 curated enabled + 1 个 curated disabled + 1 个 project → 返回 3 条, enabled 字段正确 (2) `kind: "bundled"` entry 行为不变] curated 桶并入扫描 + enabled 过滤。
- [ ] 2.3 [P0][depends:2.2][I: `src-tauri/src/types.rs` 的 `AppSettings` 定义 + design Decision 3 命名 + Decision 4 不进 restart 判定][O: `AppSettings` 增字段 `#[serde(default, rename = "enabledCuratedSkillIds")] enabled_curated_skill_ids: Vec<String>`, `Default` 为空 `vec![]`; **不修改** `app_settings_change_requires_codex_restart`(`src-tauri/src/shared/settings_core.rs:299-321`); **新增** 1 个 unit test 守住新字段不触发 restart][V: `cargo test` 覆盖 (1) `AppSettings::default()` 字段为空 (2) serde 序列化后字段名 `enabledCuratedSkillIds` (3) 反序列化缺失字段 fallback 空 (4) `app_settings_change_requires_codex_restart(&default, &with_curated)` 返回 false] AppSettings 字段 + restart 守护。
- [ ] 2.4 [P0][depends:2.3][I: `src-tauri/src/command_registry.rs` 现有 IPC 注册 pattern + design Decision 6 沿用 updateAppSettings 返新 AppSettings pattern][O: 注册 4 个 IPC: `get_curated_skills() -> Vec<CuratedSkillInfo>`(含 enabled 字段) / `set_curated_skill_enabled(skill_id: String, enabled: bool) -> Result<AppSettings, String>`(写 AppSettings + **返回新 AppSettings, 不 emit 任何事件**); **handler 内部必须**调 `update_app_settings_core(new_settings, &state.app_settings, &state.settings_path)` 走核心, **不调** `update_app_settings` 整 IPC(避免触发 `app_settings_change_requires_codex_restart` 误判 + 跳过 `apply_window_appearance` 副作用); daemon 端 handler 同样调 core 不调 full IPC / `get_enabled_curated_skill_ids() -> Vec<String>` / `get_curated_skill_bodies() -> Vec<(String, String)>`; `lib.rs` 的 `invoke_handler` 注册; **不新增** `emit_all` 事件, 沿用 `updateAppSettings` 已有 pattern][V: 单元测试: (1) set + get 状态一致 (2) set 返回的 AppSettings.enabled_curated_skill_ids 正确 (3) skill_id 不存在 → Err (4) 0 个 emit 调用(用 mock AppHandle 验证)] IPC + 不新增事件。
- [ ] 2.5 [P0][depends:2.4][I: `src-tauri/src/backend/app_server_cli.rs:586-617` 现有 `codex_args_contain_instruction_override` + `encode_toml_string` + `src-tauri/src/codex/collaboration_policy.rs:171` `merge_developer_instructions` + design Decision 5 Codex 注入][O: 新增 2 个工具函数(放 `app_server_cli.rs`): (a) `extract_existing_developer_instructions(args: &[String]) -> Option<String>` 解析 user `codex_args` 里的 `-c developer_instructions=...` / `--config developer_instructions=...` / `-c instructions=...` 形式返回 unescaped existing; (b) `codex_curated_skills_config_arg(app_settings: &AppSettings, existing: Option<&str>) -> Option<String>` 主拼装; **集成到 `build_codex_app_server_args` 末尾** 走 N7 完整 merge 流程: `extract_existing_developer_instructions(&args)` → `list_enabled_curated_skill_bodies(&app_settings).into_iter().map(|(id, body)| format!("<skill id=\"{id}\">\n{body}\n</skill>")).collect::<Vec<_>>()` 作 directives → `merge_developer_instructions(existing, &curated_directives)` → 若 `Some(merged)` 则 `args.push("-c"); args.push(format!("developer_instructions={}", encode_toml_string(&merged)))`, 0 个 enabled 时不追加][V: 单元测试: (1) 0 个 enabled → 返回 None → args 不变 (2) 1 个 enabled → args 末尾追加 -c developer_instructions=... (3) 已有 user developer_instructions= → 不追加 (4) TOML escape 正确处理 换行/双引号/反斜杠] Codex 注入。
- [ ] 2.6 [P0][depends:2.5][I: `src-tauri/src/engine/claude.rs:814` `build_command` + `claude.rs:386-392` "Keep user prompt bytes out of argv" 注释 + design Decision 5 Claude 注入][O: 新增 `fn build_curated_skill_append_args(app_settings: &AppSettings) -> Option<String>`: 0 个 enabled → None; N 个 enabled → 拼一段 system prompt 文本(`## Curated Skills\n<skill id=\"...\">\nbody\n</skill>...`), **不写 temp file**; 若 body 字节数 > 100KB, 截断到 100KB 并 metadata 标 `claude-injection-truncated: true`; 在 `build_command` 里 `cmd.arg("-p")` 之后, if `append_args.is_some()` 则 `cmd.arg("--append-system-prompt").arg(append_args.unwrap())`; **不写 temp file** 也 **不破坏** user prompt 走 stdin 的现有行为][V: 单元测试: (1) 0 个 enabled → 返回 None (2) 1 个 enabled → 返回 `Some(body)`, body 含 ponytail 关键字 + 7 级 Ladder 段 (3) body 字节数 > 100KB → 截断到 100KB + metadata 标 `claude-injection-truncated: true`; **不验证 temp file**(N3 修正版: 不写 temp file)] Claude 注入。
- [ ] 2.7 [P1][depends:2.3][I: `src-tauri/src/shared/settings_core.rs` 现有 `restore_app_settings_core` / `update_app_settings_core`][O: 持久化 `enabled_curated_skill_ids` 字段(读 + 写双向); 字段缺失时 fallback 空数组; 不破坏现有 60+ 字段][V: 单元测试: (1) restore 后字段保留 (2) update 后字段写入 (3) 字段缺失不 panic] settings_core 持久化。
- [ ] 2.8 [P0][depends:2.2][I: `src-tauri/src/bin/cc_gui_daemon/daemon_state.rs:2618-2660` 现有 `skills_list` impl + 真实 evidence: daemon 调 `skills::skills_list_local_core` 需 `app_settings` 引用][O: daemon `skills_list` handler 同步 Tauri 路径: 把 `app_settings` 引用传给 `skills::skills_list_local_core`(签名已在 task 2.2 扩, 此处只调 1 行); 返回的 JSON 加 `enabled` 字段(行为与 Tauri 路径完全一致); fallback 到 `codex_core::skills_list_core(&self.sessions, ...)` 路径时 JSON 也要含 `enabled` 字段(默认 true)][V: 集成测试: (1) daemon 与 Tauri 路径对同一 `AppSettings` 返回的 JSON 字段一致 (2) daemon `enabled` 字段反映 `enabled_curated_skill_ids` (3) fallback 路径含 `enabled: true` 默认值] Daemon 路径一致性(S2 修复)。
- [ ] 2.9 [P0][depends:2.3][I: `src-tauri/src/shared/settings_core.rs:299-321` `app_settings_change_requires_codex_restart` + design Decision 4][O: **不修改** 函数体; 在 `src-tauri/src/shared/settings_core.rs` 现有 test 段(`#[cfg(test)] mod tests`) 新增 3 个 case: (1) `enabled_curated_skill_ids` 从空 → 1 个 → 返回 false; (2) `enabled_curated_skill_ids` 从 1 个 → 2 个 → 返回 false; (3) `enabled_curated_skill_ids` 从 N 个 → 空 → 返回 false; 还要在 daemon `daemon_state.rs:605` 调用点加同样守护 test][V: 3 个新 case 全 pass; daemon 端 case 也 pass; `git diff` 显示函数体未改, 只 test 段新增] Restart 守护(S5 修复)。

## 3. Frontend Layer (Settings UI + Composer chip 行 + Picker)

- [ ] 3.1 [P0][depends:2.4][I: 现有 `src/services/tauri.ts` 模式 + `useAppSettings` hook 已有 `updateSettings` setter pattern + design Decision 6][O: 增 `getCuratedSkills()` / `setCuratedSkillEnabled(skillId, enabled): Promise<AppSettings>` / `getEnabledCuratedSkillIds()` / `getCuratedSkillBodies()` 4 个 invoke wrapper; `src/types/index.ts` 增 `CuratedSkillOption`(name/displayName/icon/category/tokenEstimate/version/license/enabled) + 复用 `AppSettings.enabledCuratedSkillIds: string[]`][V: `npm run typecheck` 退出码 0] 前端 IPC 类型 + wrapper。
- [ ] 3.2 [P0][depends:3.1][I: `src/features/skills/hooks/useSkills.ts` 现有 `useEffect` + `startupOrchestrator` 模式 + design Decision 7 CuratedSection 数据源走 useSkills 过滤][O: 新增 `src/features/curated-skills/hooks/useCuratedSkills.ts`: 不新调 IPC, 直接 `const all = useSkills(...); return all.skills.filter(s => s.source === "curated_bundled")`; 新增 `useCuratedSkillToggle.ts`: 封装 `set_curated_skill_enabled` IPC, 调用后 `useAppSettings().updateSettings(newSettings)` 触发 React 重渲染][V: `useCuratedSkillToggle.test.ts` 覆盖 (1) setEnabled 调 IPC (2) 收到新 AppSettings 后 updateSettings 被调用 (3) skillId 不存在 → throw] curated skills hook。
- [ ] 3.3 [P0][depends:3.2][I: `src/features/settings/components/SkillsSection.tsx` 1100+ 行, **不复用**, new `CuratedSection` 并列 + design Decision 7 + lucide-react `import Sparkles from "lucide-react/dist/esm/icons/sparkles"` 路径][O: 新增 `src/features/skills/components/CuratedSection.tsx`: 桶头 "📦 Curated" + 来源说明 "客户端内置, 发版打包, 零网络"; 行用 lucide-react `Sparkles`(kebab-case `sparkles`)+ displayName + description(2 行截断) + tokenEstimate(灰色 "≈1.1K tokens") + Toggle; 数据源 `useCuratedSkills()`; toggle 接 `useCuratedSkillToggle.setEnabled`; 不复用 SkillsSection 内部任何逻辑(不引入 if-else 分支); Vitest 覆盖 (1) 切换后 AppSettings 同步 (2) chip 行同步 (3) 系统未启动 curated 时 section 显示空状态][V: `CuratedSection.test.tsx` 覆盖 (1) 渲染 curated entry (2) toggle 切换调 IPC (3) AppSettings 同步 (4) 0 个 curated 时显示空状态] CuratedSection 组件。
- [ ] 3.4 [P0][depends:3.3][I: `src/features/settings/components/SettingsView.tsx:2314-2317` 现有 `SkillsSection` 渲染位置 + design Decision 7 并列渲染][O: 改 `SettingsView.tsx:2314-2317` 在 `<SkillsSection ... />` 之前插一段 `<CuratedSection ... />`; CuratedSection 接收的 props 与 SkillsSection 类似 (`activeWorkspace` / `appSettings` / `onUpdateAppSettings`); **不修改** SkillsSection 内部任何代码][V: `SettingsView.test.tsx` 覆盖 (1) CuratedSection 出现在 SkillsSection 之前 (2) SkillsSection 现有 test 全 pass (3) CuratedSection 0 个 entry 时不渲染桶头] SettingsView 整合。
- [ ] 3.5 [P0][depends:3.2][I: `src/features/composer/components/ChatInputBox/index.tsx` 现有 `ChatInputBoxFooter` / `MessageQueue` 子组件位置 + design Decision 8 chip 行数据源走 useAppSettings][O: 新增 `src/features/curated-skills/components/CuratedSkillChipRow.tsx`: 从 `useAppSettings().settings.enabledCuratedSkillIds` 拿 `Set<string>`; 渲染 0 个时不输出任何 DOM; 右侧 `+` 按钮触发 `<CuratedSkillPicker />`; chip 显示 icon + displayName, hover tooltip 含 tokenEstimate + "下次发送生效"; 改 `ChatInputBox/index.tsx` 在 `ChatInputBoxFooter` 之前(或 MessageQueue 之后, 以现有 DOM 顺序为准)插入 `<CuratedSkillChipRow />`; **不订阅** 后端事件, 只读 useAppSettings state][V: `CuratedSkillChipRow.test.tsx` 覆盖 (1) 0 个 enabled 时不渲染 (2) 1 个 enabled 时 1 个 chip (3) `+` 触发 picker] Composer chip 行。
- [ ] 3.6 [P0][depends:3.5][I: 现有 popover 组件 `src/features/composer/components/ComposerContextMenuPopover.tsx`(已存在, anchor ref + onClose + placement) + design Decision 7 复用 + Decision 5 复用][O: 新增 `src/features/curated-skills/components/CuratedSkillPicker.tsx`: 复用 `ComposerContextMenuPopover` 作为容器, 行布局 icon + displayName + description(单行截断) + tokenEstimate + Toggle; 顶部 status bar 显示 "已加载 X / Y tokens, 上限 8000" + 超 5000 警告(amber); 超 8000 阻止 enable; 顶部 status bar 数字从 `useAppSettings().settings.enabledCuratedSkillIds` 算总 token(查 metadata.json)][V: `CuratedSkillPicker.test.tsx` 覆盖 (1) 显示所有 curated skill (2) toggle 切换 (3) status bar 数字正确 (4) 超 5000 amber 样式 (5) 超 8000 阻止 enable] Picker popover。

## 4. Documentation / Onboarding

- [ ] 4.1 [P0][depends:1.5][I: 1.5 落地的 onboarding.md + proposal §4 真实回退路径 (a)(b)(c)][O: 新增 `openspec/changes/2026-06-24-curated-skill-bundles/docs/onboarding-checklist.md`, PR review 时按此 checklist 走: SKILL.md 顶部 attribution 注释 / metadata.json schema 字段 / sha256 重算与 lock 一致 / license 在白名单(无 MPL-2.0) / icon kebab-case ASCII / category 在 MVP 4 个枚举 / tokenEstimate ≤ 3000 / 不与已有 curated / skills-lock skill 命名冲突 / 含"何时不启用"反向说明][V: 文件存在, 9 项 checklist 段落完整] PR review checklist。
- [ ] 4.2 [P1][depends:4.1][I: 1.5 文档 + design §风险与权衡][O: `docs/curated-skill-onboarding.md` 补"已知边界 / 后续 V1.1 任务"段: (1) lucide-react icon 完整白名单; (2) MPL-2.0 license 准入走法律评审; (3) 中文 token 估算精度(`tiktoken-rs` 升级); (4) category 枚举扩到 8 个; (5) Composer `@` 触发 curated skill picker][V: 文档存在 5 个 follow-up 段落] V1.1 follow-up 文档化。

## 5. Verification And Hardening

- [ ] 5.1 [P0][depends:3.6][I: 全栈已落盘代码][O: `npm run typecheck` 退出码 0 + `npm run lint` 退出码 0 + `npm run test` 全绿 + `cargo test --manifest-path src-tauri/Cargo.toml` 全绿 + `cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings` 0 warning][V: 5 个命令退出码全 0] 仓库级硬门禁。
- [ ] 5.2 [P0][depends:5.1][I: 3.4 + 3.5 落盘 UI + design Decision 5 mock CLI argv 验证路径 + proposal Gate 2 修正版][O: 写 `tests/curated-skills-e2e.test.tsx`(Vitest + mock `find_cli_binary` 走"echo args" stub, 用 `tokio::process::Command` mock): 场景 (1) 启动 → 进 Settings → 看到 Curated 桶在 SkillsSection 之前, 1 个 entry 默认 off; (2) 点 toggle on → chip 行出现 chip; (3) 进 composer 输入 "hi" 发送; (4) **Codex**: 抓 `build_codex_app_server_args` 返回的 `args` 列表, 断言含 `-c developer_instructions="...ponytail 7 级 Ladder..."`(若启用了 curated)或不出现该参数(若禁用); (5) **Claude**: 抓 `build_command` 返回的 argv, 断言含 `--append-system-prompt "<body>"`, argv 第二个值为拼接后的 system prompt 字符串(直接传字符串, **不写 temp file**), body 含 `7 级 Ladder`; (6) 关 toggle → chip 消失 → 下次发送 argv 不含 curated 段][V: e2e 6 步全 pass, 录屏留档 `docs/perf/v0.5.14-curated-skill-e2e.mp4`] E2E 行为验证(B2 修复, 用 mock CLI argv 替代"system prompt 末尾含关键字" 不可观测断言)。
- [ ] 5.3 [P0][depends:5.1][I: 1.3 build.rs 校验逻辑 + 1.2 `kind: "bundled"` 跳过规则][O: 故意篡改 `SKILL.md` 一行 → `cargo check` 应失败且 stderr 含 `curated skill lock hash mismatch` + 期望 hash + 实际 hash; 改回后 `cargo check` 0; `cargo build --release` 同样行为; **另外**故意篡改一个 `kind: "bundled"` entry 的 `computedHash` → `cargo check` **不报错** (证明只校验 curated)][V: 篡改 curated → fail, 改回 → 0; 篡改 bundled → 0; release build 不退化] 篡改 hash 硬失败 + bundled 跳过。
- [ ] 5.4 [P0][depends:5.1][I: 2.3 AppSettings + 2.7 settings_core 持久化 + 2.9 restart 守护][O: 集成测试 (Rust): set 1 个 enabled → 序列化 → 反序列化 → 状态一致; 写 `config.json` → 启动新 process → restore → enabled 保留; **再追加**: `app_settings_change_requires_codex_restart(&default, &with_curated)` 必须返回 false (3 个 case: 空→1, 1→2, N→0)][V: 4 步集成测试全 pass, 中间文件可读; 3 个 restart 守护 case 全 pass] 持久化往返一致 + restart 守护。
- [ ] 5.5 [P0][depends:5.1][I: 2.5 + 2.6 prompt 注入 + IPC `get_curated_skill_bodies` + 2.8 daemon 一致性][O: 集成测试 (Rust): 设置 2 个 enabled → `codex_curated_skills_config_arg` 返回 `Some(...)` 包含 2 个 `<skill id=...>` 段 + id 顺序与 enabled 一致 + 走 TOML escape; 设置 0 个 enabled → 返回 `None` → `build_codex_app_server_args` 不追加 -c 参数; Claude `build_curated_skill_append_args` 同样 2 场景 + 100KB 截断 case; **daemon 与 Tauri 路径**对同一 `AppSettings` 返回的 JSON 字段(含 `enabled`)完全一致][V: 6 场景全 pass, 字段比对用 `assert_eq!`] 注入契约 + Daemon 一致性(S2 修复)。
- [ ] 5.6 [P1][depends:5.5][I: design Decision 10 经验值 chars/3][O: 写脚本 `scripts/curated-skill-token-check.sh`: 对每个 curated skill 用 `tiktoken` (Python `pip install tiktoken`) 真算 cl100k_base token, 与 `metadata.json.tokenEstimate` 对比, 偏差 > 30% 报错(只 fail V1.1, V0.5.14 仅 warning 留信号)][V: 脚本输出偏差 < 30% 或仅 warning] Token 估算偏差监测。
- [ ] 5.7 [P0][depends:5.1][I: proposal Gate 4 不回归 + 2.8 daemon 一致性 + 2.9 restart 守护][O: 跑 `src-tauri/src/skills.rs` 现有 12 source 单元测试 + `useSkills` 现有 Vitest + `app-server-event-batching` capability 性能 baseline 对比 + daemon `skills_list` handler 测试(S2 修复) + restart 守护 3 case(S5 修复); 任何 case fail 视为不回归失败][V: 12 source 测试 + useSkills Vitest + performance baseline + daemon 一致性 + restart 守护 全绿, 与 0.1 baseline 对比 0 退化] 不回归硬门禁。
- [ ] 5.8 [P0][depends:5.2][I: 1.4 资源打包 + 2.5 Codex 注入 + 2.6 Claude 注入 + design Decision 5 已知风险 4 Claude CLI flag 支持][O: 一次 `npm run tauri:dev` 跨 macos aarch64 dev 模式(mvp 不强求 4 个真实 release build)成功启动 → 进 Settings → 看到 Curated 桶在 SkillsSection 之前 → toggle on → composer 发送 → 抓 Codex 子进程 argv(看 `app_server_cli.rs:649` 实际 spawn 时的 args)含 `-c developer_instructions="...ponytail..."`; Claude 抓子进程 argv(看 `claude.rs:1028` `cmd.spawn()`)含 `--append-system-prompt "<body>"`; **MVP 默认 body 必成功**(Claude CLI 真实支持 `--append-system-prompt`, 已在 `claude --help` 验证); 若 body 字节数超 100KB, metadata 标 `claude-injection-truncated: true`], 录屏留档 `docs/perf/v0.5.14-curated-skill-e2e-real.mp4`][V: macos dev build 启动后 4 步行为正确, Codex 注入必成功, Claude 注入成功 OR 软降级有日志] 真实启动验证(包含 Claude flag 兼容性验证)。

## 6. Archive Gate

- [ ] 6.1 [P0][depends:5.1, 5.2, 5.3, 5.4, 5.5, 5.7, 5.8][I: 全部 task 完成 + OpenSpec 校验通过 + E2E 通过 + 不回归验证通过][O: 跑 `openspec validate 2026-06-24-curated-skill-bundles --strict --no-interactive` 退出码 0 → `openspec archive 2026-06-24-curated-skill-bundles` 同步 spec delta 到 main spec (`openspec/specs/curated-skill-bundles/spec.md`); 把 `docs/onboarding-checklist.md` 迁到 `docs/curated-skill-onboarding.md`; 在 `openspec/project.md` 的 active changes 表里移除本 change][V: 4 步命令全退出码 0, `openspec/specs/curated-skill-bundles/spec.md` 内容已 sync, project.md 已更新] Archive 闭环。
